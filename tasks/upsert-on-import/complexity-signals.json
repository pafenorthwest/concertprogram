{
  "scope": 2,
  "behavior": 2,
  "dependency": 0,
  "uncertainty": 2,
  "verification": 2,
  "evidence": {
    "scope": "files=3 surface=import logic + db tests + task artifacts within one subsystem",
    "behavior": "one workflow updated: import retries must reuse an existing performance row",
    "dependency": "interfaces=none",
    "uncertainty": "partial-failure recovery path with moderate risk of stale row state",
    "verification": "checks=lint,build,test plus targeted db import coverage",
    "guardrails": "no schema or API contract change; no new deps; localized server-side surface; rollback is straightforward with bounded verification"
  },
  "guardrails": {
    "no_schema_or_api_contract_change": true,
    "no_new_external_dependencies": true,
    "localized_surface": true,
    "reversible_with_straightforward_verification": true
  },
  "overrides": {
    "reason": ""
  }
}
